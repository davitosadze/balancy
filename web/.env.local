# Server-side URL used by the Next.js proxy rewrite
DIRECTUS_URL=http://localhost:8055
# Client-side relative URL — all Directus requests go through the Next.js proxy (no CORS)
NEXT_PUBLIC_DIRECTUS_URL=/directus
