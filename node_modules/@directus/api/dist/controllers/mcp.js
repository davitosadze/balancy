import async_handler_default from "../utils/async-handler.js";
import { SettingsService } from "../services/settings.js";
import { DirectusMCP } from "../ai/mcp/server.js";
import "../ai/mcp/index.js";
import { ForbiddenError } from "@directus/errors";
import { Router } from "express";

//#region src/controllers/mcp.ts
const router = Router();
const mcpHandler = async_handler_default(async (req, res) => {
	const settings = new SettingsService({ schema: req.schema });
	const { mcp_enabled, mcp_allow_deletes, mcp_prompts_collection, mcp_system_prompt, mcp_system_prompt_enabled } = await settings.readSingleton({ fields: [
		"mcp_enabled",
		"mcp_allow_deletes",
		"mcp_prompts_collection",
		"mcp_system_prompt",
		"mcp_system_prompt_enabled"
	] });
	if (!mcp_enabled) {
		throw new ForbiddenError({ reason: "MCP must be enabled" });
	}
	const mcp = new DirectusMCP({
		promptsCollection: mcp_prompts_collection,
		allowDeletes: mcp_allow_deletes,
		systemPromptEnabled: mcp_system_prompt_enabled,
		systemPrompt: mcp_system_prompt
	});
	mcp.handleRequest(req, res);
});
router.get("/", mcpHandler);
router.post("/", mcpHandler);
var mcp_default = router;

//#endregion
export { mcp_default as default };