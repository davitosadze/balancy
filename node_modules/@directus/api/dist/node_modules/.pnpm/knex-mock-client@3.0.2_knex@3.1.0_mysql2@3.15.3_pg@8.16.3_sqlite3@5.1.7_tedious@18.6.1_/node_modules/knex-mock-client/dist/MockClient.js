

import { __commonJS, __require } from "../../../../../../_virtual/rolldown_runtime.js";
import { require_MockConnection } from "./MockConnection.js";

//#region ../node_modules/.pnpm/knex-mock-client@3.0.2_knex@3.1.0_mysql2@3.15.3_pg@8.16.3_sqlite3@5.1.7_tedious@18.6.1_/node_modules/knex-mock-client/dist/MockClient.js
var require_MockClient = /* @__PURE__ */ __commonJS({ "../node_modules/.pnpm/knex-mock-client@3.0.2_knex@3.1.0_mysql2@3.15.3_pg@8.16.3_sqlite3@5.1.7_tedious@18.6.1_/node_modules/knex-mock-client/dist/MockClient.js": ((exports) => {
	var __importDefault = exports && exports.__importDefault || function(mod) {
		return mod && mod.__esModule ? mod : { "default": mod };
	};
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.MockClient = void 0;
	const knex_1 = __importDefault(__require("knex"));
	const MockConnection_1 = require_MockConnection();
	var MockClient = class extends knex_1.default.Client {
		isMock = true;
		constructor(config) {
			super(config);
			if (config.dialect) {
				this._attachDialectQueryCompiler(config);
			}
		}
		acquireConnection() {
			return Promise.resolve(new MockConnection_1.MockConnection());
		}
		releaseConnection() {
			return Promise.resolve();
		}
		processResponse(response) {
			return response;
		}
		setTracker(tracker) {
			this.driver = this.driver || {};
			this.driver.tracker = tracker;
		}
		_query(connection, rawQuery) {
			let method = rawQuery.method;
			const rawMethod = rawQuery.method;
			switch (rawMethod) {
				case "first":
				case "pluck":
					rawQuery.postOp = rawMethod;
					method = "select";
					break;
				case "del":
					method = "delete";
					break;
				case "raw":
					method = rawQuery.sql.toLowerCase().trim().split(" ").shift();
					break;
			}
			const tracker = this.driver?.tracker;
			if (!tracker) {
				throw new Error("Tracker not configured for knex mock client");
			}
			return tracker._handle(connection, {
				...rawQuery,
				method
			});
		}
		_attachDialectQueryCompiler(config) {
			const { resolveClientNameWithAliases } = __require("knex/lib/util/helpers");
			const { SUPPORTED_CLIENTS } = __require("knex/lib/constants");
			if (!SUPPORTED_CLIENTS.includes(config.dialect)) {
				throw new Error(`knex-mock-client: Unknown configuration option 'dialect' value ${config.dialect}.\nNote that it is case-sensitive, check documentation for supported values.`);
			}
			const resolvedClientName = resolveClientNameWithAliases(config.dialect);
			const Dialect = __require(`knex/lib/dialects/${resolvedClientName}/index.js`);
			const dialect = new Dialect(config);
			Object.setPrototypeOf(this.constructor.prototype, dialect);
		}
	};
	exports.MockClient = MockClient;
}) });

//#endregion
export default require_MockClient();

export { require_MockClient };