

import { __commonJS } from "../../../../../../_virtual/rolldown_runtime.js";

//#region ../node_modules/.pnpm/knex-mock-client@3.0.2_knex@3.1.0_mysql2@3.15.3_pg@8.16.3_sqlite3@5.1.7_tedious@18.6.1_/node_modules/knex-mock-client/dist/MockConnection.js
var require_MockConnection = /* @__PURE__ */ __commonJS({ "../node_modules/.pnpm/knex-mock-client@3.0.2_knex@3.1.0_mysql2@3.15.3_pg@8.16.3_sqlite3@5.1.7_tedious@18.6.1_/node_modules/knex-mock-client/dist/MockConnection.js": ((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	exports.MockConnection = void 0;
	var Stack = class {
		values = [];
		push(value) {
			this.values.push(value);
		}
		pop() {
			const [lastItem] = this.values.splice(this.values.length - 1);
			return lastItem;
		}
		/**
		* Peek at a value on the stack without removing it.
		*
		* @param offset {number} Offset from the top of the stack to peek. Default to 0.
		*/
		peek(offset = 0) {
			return this.values[this.values.length - offset - 1];
		}
		/**
		* Points the stack to the first occurrence of the given value.
		*
		* If the given value is not on the stack, push it to the top.
		* If the given value is already in the stack, remove all the transactions above it.
		* If the given value is undefined, clears the stack.
		*/
		pointTo(value) {
			if (this.peek() === value) return;
			if (value === undefined) {
				this.values.splice(0);
				return;
			}
			const index = this.values.indexOf(value);
			if (index >= 0) {
				this.values.splice(index + 1);
			} else {
				this.values.push(value);
			}
		}
	};
	var MockConnection = class {
		__knexUid = Math.trunc(Math.random() * 1e6);
		fakeConnection = true;
		transactionStack = new Stack();
		beginTransaction(cb) {
			cb();
		}
		commitTransaction(cb) {
			cb();
		}
	};
	exports.MockConnection = MockConnection;
}) });

//#endregion
export default require_MockConnection();

export { require_MockConnection };