import { TypeBoxError } from "../error/error.js";
import "../error/index.js";
import { Kind } from "../symbols/symbols.js";
import "../symbols/index.js";
import { IsBigInt, IsBoolean, IsInteger, IsLiteral, IsNumber, IsString, IsTemplateLiteral, IsUnion } from "../guard/kind.js";
import { PatternBoolean, PatternNumber, PatternString } from "../patterns/patterns.js";
import "../patterns/index.js";

//#region ../node_modules/.pnpm/@sinclair+typebox@0.34.41/node_modules/@sinclair/typebox/build/esm/type/template-literal/pattern.mjs
var TemplateLiteralPatternError = class extends TypeBoxError {};
function Escape(value) {
	return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function Visit(schema, acc) {
	return IsTemplateLiteral(schema) ? schema.pattern.slice(1, schema.pattern.length - 1) : IsUnion(schema) ? `(${schema.anyOf.map((schema$1) => Visit(schema$1, acc)).join("|")})` : IsNumber(schema) ? `${acc}${PatternNumber}` : IsInteger(schema) ? `${acc}${PatternNumber}` : IsBigInt(schema) ? `${acc}${PatternNumber}` : IsString(schema) ? `${acc}${PatternString}` : IsLiteral(schema) ? `${acc}${Escape(schema.const.toString())}` : IsBoolean(schema) ? `${acc}${PatternBoolean}` : (() => {
		throw new TemplateLiteralPatternError(`Unexpected Kind '${schema[Kind]}'`);
	})();
}
function TemplateLiteralPattern(kinds) {
	return `^${kinds.map((schema) => Visit(schema, "")).join("")}\$`;
}

//#endregion
export { TemplateLiteralPattern, TemplateLiteralPatternError };